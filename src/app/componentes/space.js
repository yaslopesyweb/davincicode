"use client"
import styled from "styled-components";

// Estilizar Espaço
const Space = styled.div`
   padding: 13px;
`;

// Espaço
export default function Espaco() {
    return (
            <Space></Space>
    );
}